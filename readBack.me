1. npm i nodemon express bcrypt cors dotenv mongoose socket.io validator

2. Le CORS permet de prendre en charge des requêtes multi-origines sécurisées et des transferts 
de données entre des navigateurs et des serveurs web => communication entre client et serveur.

3. La fonction express.urlencoded() est une fonction middleware intégrée dans Express. 
Il analyse les requêtes entrantes avec des charges utiles codées en URL et est basé sur 
un analyseur de corps.